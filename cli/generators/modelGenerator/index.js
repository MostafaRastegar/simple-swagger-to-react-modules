const { generateModelFiles } = require("./generator.model");
const { generateSingleModelInterface } = require("./interface.model");

module.exports = {
  generateModelFiles,
  generateSingleModelInterface,
};
