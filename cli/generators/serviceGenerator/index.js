const { generateServiceImplementation } = require("./generator.service");
const { generateServiceInterface } = require("./interface.service");

module.exports = {
  generateServiceInterface,
  generateServiceImplementation,
};
