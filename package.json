{
  "name": "swagger-to-modules",
  "version": "1.0.0",
  "description": "A real-world `DDD` implementation project comprises the same phases as any other software development project. These phases include:",
  "main": "index.js",
  "dependencies": {
    "commander": "^12.1.0",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "export": "node cli/cli.js generate -m $npm_config_tag -i swagger.json -o src/modules/",
    "generate": "node scripts/select-module.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
